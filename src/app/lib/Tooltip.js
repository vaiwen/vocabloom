import React from 'react'

const Tooltip = () => {
  return (
    <p className='absolute -top-10 -right-10 p-2 rounded-lg text-xs text-white bg-blue-600 font-semibold transition-all duration-300 max-w-[200px]'>choose  a card in the "All Cards" for delete, selected card comes to top.</p>
  )
}

export default Tooltip